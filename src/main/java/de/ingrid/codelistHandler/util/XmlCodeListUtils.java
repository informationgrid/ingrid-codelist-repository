package de.ingrid.codelistHandler.util;


public class XmlCodeListUtils {

    /*
    @SuppressWarnings("unchecked")
    public static List<CodeList> readFromXml(String path) throws FileNotFoundException {
        XStream xStream = new XStream();
        return (List<CodeList>) xStream.fromXML(new FileInputStream(path));
    }
    
    public static void writeToXml(String path, List<CodeList> data, FileOutputStream out) {
        XStream xStream = new XStream();
        xStream.toXML(data, out);
    }
    
    public static void writeToDB(String path, List<CodeList> data, Object dbInfo) {
        
    }*/
}
